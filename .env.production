# Public, not a secret: the Reado service the account pages call from the browser.
# Read by `next build` (static export inlines NEXT_PUBLIC_* at build time).
NEXT_PUBLIC_API_URL=https://api.reado.watermelon-studio.it
